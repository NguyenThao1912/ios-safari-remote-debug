# Proxy host for ios_webkit_debug_proxy
# Default: 127.0.0.1:9221
# If running on remote machine, set to that machine's IP:port
PROXY_HOST=127.0.0.1:9221

# Domain name for Caddy (optional, can be set in Caddyfile)
DOMAIN=localhost

